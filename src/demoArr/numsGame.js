/**
 * @param {number[]} nums
 * @return {number[]}
 */
var numsGame = function (nums) {
  // 给出一个数组（排序不定）
  // 将其设计为步长为1的等差升序数列
  // 要求是所有方案里的最小值

  // 首先枚举有办法么
  for (let i = 0; i < nums.length; i++){
    // 难点在于，这有两个选择，+和-
  }
}

let nums = [3, 4, 5, 1, 6, 7]
numsGame(nums)
